name: CI - Build & Test Spring Boot Backend

on:
  push:
    branches: [ main ]
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Java
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Build app
        working-directory: backend
        run: mvn clean package -DskipTests

      - name: Create .env file
        working-directory: backend
        run: |
          echo "DATABASE_URL=${{ secrets.DATABASE_URL }}" > .env
          echo "KAFKA_SERVERS=${{ secrets.KAFKA_SERVERS }}" >> .env
          echo "KAFKA_USERNAME=${{ secrets.KAFKA_USERNAME }}" >> .env
          echo "KAFKA_PASSWORD=${{ secrets.KAFKA_PASSWORD }}" >> .env
          echo "DATABASE_USER=${{ secrets.DATABASE_USER }}" >> .env
          echo "REDIS_HOST=${{ secrets.REDIS_HOST }}" >> .env
          echo "REDIS_PASSWORD=${{ secrets.REDIS_PASSWORD }}" >> .env
          echo "GOOGLE_APP_USERNAME=${{ secrets.GOOGLE_APP_USERNAME }}" >> .env
          echo "GOOGLE_APP_PASSWORD=${{ secrets.GOOGLE_APP_PASSWORD }}" >> .env
          echo "KAFKA_SERVER=${{ secrets.KAFKA_SERVER }}" >> .env
          echo "WEATHER_APP_KEY=${{ secrets.WEATHER_APP_KEY }}" >> .env
          echo "DATABASE_PASSWORD=${{ secrets.DATABASE_PASSWORD }}" >> .env
          echo "DATABASE_ACTIVE=${{ secrets.DATABASE_ACTIVE }}" >> .env
      

      - name: Copy JAR and .env to EC2
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USERNAME }}
          key: ${{ secrets.EC2_SSH_KEY }}
          source: |
            backend/target/*.jar
            backend/.env
          target: "/home/ec2-user/dailysync/"

      - name: SSH and deploy on EC2
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USERNAME }}
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            pkill -f 'java -jar' || echo "Nothing to kill"
            mkdir -p ~/home/ec2-user/dailysync/backend/target/
            mv ~/app/*.jar ~/home/ec2-user/dailysync/backend/target/app.jar
            mv ~/app/.env ~/home/ec2-user/dailysync/backend/target/.env
            cd ~//home/ec2-user/dailysync/backend/target/
            nohup java -jar app.jar > app.log 2>&1 &

